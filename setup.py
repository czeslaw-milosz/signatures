import setuptools

setuptools.setup(
    name="signatures",
    version="0.0.0",
    description="Python package for cancer mutational signature extraction (work in progress)",
    author="Radosław Jurczak",
    author_email="r.jurczak@uw.edu.pl",
    url="https://github.com/czeslaw-milosz/signatures",
    packages=setuptools.find_packages(),
    install_requires=[],
    setup_requires=["wheel"]
)
